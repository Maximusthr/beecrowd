#include <bits/stdc++.h>

using namespace std;

int main(){
    int qtd; cin >> qtd;

    for (int i = 0; i < qtd; i++){
        vector<int> seq;
        int x; cin >> x;

        for (int j = 0; j < x; j++){
            int aux; cin >> aux;
            seq.push_back(aux);
        }
        ////
    }
}
