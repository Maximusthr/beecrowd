#include <bits/stdc++.h>

using namespace std;

int main(){
    int x, y; cin >> x >> y;

    if (x == 0) cout << "C";
    else if (x == 1 && y == 1) cout << "A";
    else cout << "B";
    cout << "\n";
}
